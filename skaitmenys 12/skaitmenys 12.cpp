#include <iostream>
#include <fstream>
#include <iomanip>
#include <cmath>

using namespace std;

int main ()
{
    int n, liek;
    cout << n;
    cin >> n;
    while(n>0)
    {
        liek = n%10;
        {
            if()
        }
    }

    return 0;
}
